<div class="max-w-4xl mx-auto p-6" data-controller="items">
  <div class="flex justify-between items-center mb-8">
    <h1 class="text-3xl font-bold text-gray-800">Del 1: Objekt- og Meterregistrering</h1>
    <%= link_to "Logg ut", logout_path, method: :delete, class: "bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-md text-sm" %>
  </div>

  <%= form_with model: @agreement, url: storage_items_path, method: :post, class: "space-y-6", id: "items_form" do |f| %>
    <div id="storage_items" class="space-y-4">
      <% @storage_items.each do |item| %>
        <%= f.fields_for :storage_items, item do |item_f| %>
          <%= render "item", f: item_f %>
        <% end %>
      <% end %>
    </div>

    <template data-items-target="itemTemplate">
      <%= f.fields_for :storage_items, StorageItem.new, child_index: "NEW_RECORD" do |item_f| %>
        <%= render "item", f: item_f %>
      <% end %>
    </template>

    <div class="flex gap-4 mt-6">
      <%= button_tag "+ Legg til objekt", 
            type: "button",
            data: { action: "click->items#addItem" },
            class: "bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-md" %>
      <%= f.submit "Lagre og videre til Del 2", 
            class: "bg-green-500 hover:bg-green-600 text-white px-6 py-2 rounded-md" %>
    </div>
  <% end %>

  <div class="mt-8 p-4 bg-gray-50 rounded-lg">
    <h3 class="text-lg font-semibold mb-2">Sammendrag</h3>
    <p class="text-gray-700">Total meter: <span id="total_meters"><%= @agreement.total_meters || 0 %></span></p>
    <p class="text-gray-700">Total pris: <span id="total_price"><%= number_to_currency(@agreement.total_price || 0) %></span></p>
  </div>
</div>
